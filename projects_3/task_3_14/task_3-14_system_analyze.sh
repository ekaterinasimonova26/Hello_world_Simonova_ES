#!/bin/bash

echo "=== Файловая система и процент заполнения ==="
df -h | awk 'NR>1 {
    gsub(/%/, "", $5);
    print $1, $5"%";
    if ($5 > 90) print "ПРЕДУПРЕЖДЕНИЕ: " $1 " заполнена на " $5 "%!"
}'
